{% extends 'base.html.twig' %}

{% block body %}
    <div class="max-w-2xl mx-auto my-12">
        {{ form_start(form) }}
            {{ form_widget(form) }}

            <button class="block w-full text-sm py-2 px-4 rounded-lg font-semibold bg-gray-900 text-white hover:bg-gray-800" formnovalidate>Ajouter</button>
        {{ form_end(form) }}
    </div>
{% endblock %}
